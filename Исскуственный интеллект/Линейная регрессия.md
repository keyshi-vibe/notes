Модель имеет на входе множество признаков $X = {x_1, ... , x_n}$ и таргеты $y$. 
Модель линейной регрессии (или же гипотезы) записывается как $$h_w(x) = w_0 + w_1x_1 + ... w_nx_n$$
где $w_i$ - веса признаков, а $w_0$ - смещение